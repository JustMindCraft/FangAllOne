import {HomeBanner} from '../../models/';

export default [
    {
        method: 'PATCH',
        path: '/home_banner',
        handler: (request, h) => {
            // let updataimg= await HomeBanner.insertOrUpdate()
        }
    },
]